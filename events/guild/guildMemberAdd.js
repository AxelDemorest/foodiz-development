const { MessageEmbed } = require("discord.js");

module.exports = {
    name: 'guildMemberAdd',
    execute(member, client) {

        /* const embed = new MessageEmbed()
            .setColor("#f87359")
            .setAuthor(member.user.tag, member.user.avatarURL())
            .setDescription(`<a:B4:836174460637675570> Nous te souhaitons la bienvenue sur **${member.guild.name}** ! <a:B13:836174165274394655>`)

        client.channels.cache.get("827926646938009691").send(`${member.user}`);
        client.channels.cache.get("827926646938009691").send(embed); */

    },
};